import { useEffect, useState } from 'react'
import { ArrowUpRight, Bot, Boxes, Database, GitBranch, Network, Plus, Rocket, Sparkles } from 'lucide-react'
import { Link } from 'react-router-dom'
import { WorkspaceLayout } from '@/components/workspace/WorkspaceLayout'
import { Card } from '@/components/ui/card'
import { Badge } from '@/components/ui/badge'
import { Button } from '@/components/ui/button'
import { useAuth } from '@/auth/AuthProvider'
import { listMcps, type Mcp } from '@/lib/api'

const actions = [{ label: 'Explore agents', text: 'Browse specialists for your workflows.', icon: Bot, href: '#agents', staged: true }, { label: 'Create an agent', text: 'Compose a new intelligent worker.', icon: Sparkles, href: '#agents', staged: true }, { label: 'Add MCP server', text: 'Connect tools and context.', icon: Network, href: '/mcp-servers' }, { label: 'Create workflow', text: 'Automate a repeatable process.', icon: GitBranch, href: '#workflows', staged: true }, { label: 'Deploy an agent', text: 'Ship intelligence into production.', icon: Rocket, href: '/deployments', staged: true }, { label: 'Add knowledge base', text: 'Ground agents in your sources.', icon: Database, href: '#knowledge', staged: true }]

export function HomePage() { const { user, token } = useAuth(); const [mcps, setMcps] = useState<Mcp[]>([]); const [loading, setLoading] = useState(true); useEffect(() => { if (!token) return; listMcps(token).then(setMcps).catch(() => setMcps([])).finally(() => setLoading(false)) }, [token]); return <WorkspaceLayout><main className="mx-auto max-w-7xl px-5 py-8 lg:px-10 lg:py-12"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="text-xs font-bold uppercase tracking-[.16em] text-blue-600 dark:text-blue-400">Workspace overview</p><h1 className="mt-3 font-display text-4xl font-semibold tracking-[-.05em] text-slate-950 dark:text-white">Good to see you, {user?.name.split(' ')[0]}.</h1><p className="mt-3 text-base text-slate-500 dark:text-slate-400">Make intelligent work part of the way your team operates.</p></div><Link to="/mcp-servers"><Button><Plus size={16} />Add a resource</Button></Link></div><div className="mt-10 grid gap-4 sm:grid-cols-3"><Stat icon={Sparkles} label="Agents" value="—" detail="Coming soon" /><Stat icon={Network} label="MCP servers" value={loading ? '…' : String(mcps.length)} detail="Connected resources" /><Stat icon={Rocket} label="Deployments" value="—" detail="Coming soon" /></div><section className="mt-12"><div className="flex items-end justify-between"><div><p className="text-xs font-bold uppercase tracking-[.16em] text-slate-400">Start building</p><h2 className="mt-2 text-xl font-bold text-slate-950 dark:text-white">Choose your next move</h2></div></div><div className="mt-5 grid gap-3 sm:grid-cols-2 xl:grid-cols-3">{actions.map(action => <ActionCard key={action.label} {...action} />)}</div></section><section className="mt-12 grid gap-5 lg:grid-cols-[1.2fr_.8fr]"><Card className="p-6"><div className="flex items-start justify-between"><div><p className="text-xs font-bold uppercase tracking-[.14em] text-slate-400">Connected resources</p><h2 className="mt-2 text-xl font-bold text-slate-950 dark:text-white">Your MCP servers</h2></div><Link to="/mcp-servers" className="text-sm font-bold text-blue-600">View all <ArrowUpRight className="inline" size={14} /></Link></div>{loading ? <p className="mt-8 text-sm text-slate-400">Loading resources...</p> : mcps.length === 0 ? <div className="mt-7 rounded-xl border border-dashed border-slate-200 p-6 text-center dark:border-slate-700"><Boxes className="mx-auto text-slate-300" size={25} /><p className="mt-3 text-sm font-semibold text-slate-500">No MCP servers connected yet.</p><Link to="/mcp-servers" className="mt-3 inline-block text-sm font-bold text-blue-600">Connect your first server</Link></div> : <div className="mt-5 space-y-2">{mcps.slice(0, 3).map(mcp => <div key={mcp.id} className="flex items-center gap-3 rounded-xl bg-slate-50 p-3 dark:bg-slate-800"><span className="flex h-9 w-9 items-center justify-center rounded-lg bg-blue-100 text-blue-600 dark:bg-blue-500/10 dark:text-blue-300"><Network size={16} /></span><span className="flex-1 text-sm font-bold text-slate-800 dark:text-slate-200">{mcp.name}</span><Badge>{mcp.status}</Badge></div>)}</div>}</Card><Card id="agents" className="bg-[#0b1220] p-6 text-white"><Sparkles className="text-blue-300" size={21} /><h2 className="mt-10 text-xl font-bold">The agent marketplace is next.</h2><p className="mt-3 text-sm leading-6 text-slate-400">Explore specialized agents, compose your own, and connect them to the resources in this workspace.</p><span className="mt-6 inline-flex rounded-full border border-slate-700 px-3 py-1.5 text-xs font-bold text-slate-400">Coming in the next phase</span></Card></section></main></WorkspaceLayout> }
function Stat({ icon: Icon, label, value, detail }: { icon: typeof Sparkles; label: string; value: string; detail: string }) { return <Card className="p-5"><Icon size={18} className="text-blue-600 dark:text-blue-300" /><p className="mt-6 text-xs font-bold uppercase tracking-[.13em] text-slate-400">{label}</p><p className="mt-1 font-display text-3xl font-semibold text-slate-950 dark:text-white">{value}</p><p className="mt-1 text-xs text-slate-400">{detail}</p></Card> }
function ActionCard({ label, text, icon: Icon, href, staged }: typeof actions[number]) { const content = <><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-slate-100 text-slate-700 dark:bg-slate-800 dark:text-slate-300"><Icon size={18} /></span><span className="mt-5 block text-sm font-bold text-slate-950 dark:text-white">{label}{staged && <span className="ml-2 rounded-full bg-slate-100 px-2 py-0.5 text-[9px] uppercase tracking-wider text-slate-400 dark:bg-slate-800">Soon</span>}</span><span className="mt-2 block text-xs leading-5 text-slate-500 dark:text-slate-400">{text}</span></>; return staged ? <Card id={label === 'Create a workflow' ? 'workflows' : undefined} className="p-5 opacity-75">{content}</Card> : <Link to={href}><Card className="h-full p-5 transition hover:-translate-y-0.5 hover:border-blue-300 hover:shadow-lg">{content}</Card></Link> }
